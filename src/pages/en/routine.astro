---
import RoutinePage from '@components/templates/RoutinePage.astro';
import { getEntry } from 'astro:content';

export const prerender = true;

const entry = await getEntry('pages', 'en/routine');
if (!entry) throw new Error('Could not find content for en/routine');
---

<RoutinePage lang="en" content={entry.data} />
