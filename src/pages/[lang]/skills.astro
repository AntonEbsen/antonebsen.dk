---
import SkillsPage from '@components/templates/SkillsPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<SkillsPage lang={lang} />
