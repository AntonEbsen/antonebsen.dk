---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<h2 class:list={["text-xs font-bold uppercase tracking-wider text-accent mb-4", className]}>
  <slot />
</h2>

