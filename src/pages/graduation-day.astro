---
import Layout from '@layouts/Layout.astro';
import menu from '../data/menu.json';

// Simple authentication/gate logic can be added later if needed
export const prerender = false;
---

<Layout title="Graduering & Reception | Anton Ebsen" description="Fejring af cand.polit graduering på Grønjordskollegiet">
  <main class="min-h-screen bg-[#0a0a0a] text-white font-sans selection:bg-accent/30">
    
    {/* Minimalist Navigation */}
    <nav class="fixed top-0 left-0 w-full z-[100] px-6 py-8 flex justify-between items-center">
        <a href="/graduation" class="text-[10px] font-black uppercase tracking-[0.3em] text-dim hover:text-accent transition-all group">
            <i class="fa-solid fa-arrow-left group-hover:-translate-x-1 transition-transform mr-2"></i> Tilbage til festen
        </a>
    </nav>

    {/* Hero Section */}
    <section class="h-screen flex flex-col items-center justify-center text-center px-6 relative overflow-hidden">
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[80vw] h-[80vw] bg-accent/5 blur-[120px] rounded-full -z-10 animate-pulse"></div>
        
        <div class="space-y-6 max-w-3xl animate-in fade-in slide-in-from-bottom-8 duration-1000">
            <div class="w-12 h-1 bg-accent/20 mx-auto rounded-full mb-8"></div>
            <h1 class="text-6xl md:text-8xl font-black tracking-tighter italic leading-[0.9]">
                Reception & <br/>
                <span class="text-accent text-glow-accent outline-text-accent">Middag</span>
            </h1>
            <p class="text-xl md:text-2xl text-dim font-medium max-w-xl mx-auto leading-relaxed pt-8">
                En hyggelig eftermiddag for familie og venner på Grønjordskollegiet, før den store fest går i gang.
            </p>
            <div class="pt-12 flex flex-col items-center gap-4">
                <p class="text-[10px] font-black uppercase tracking-[0.4em] text-dim">Gem Datoen</p>
                <p class="text-2xl font-bold italic text-white">Juni 2026</p>
            </div>
        </div>

        <div class="absolute bottom-12 left-1/2 -translate-x-1/2 animate-bounce opacity-20">
            <i class="fa-solid fa-chevron-down text-2xl"></i>
        </div>
    </section>

    {/* The Venue Section */}
    <section class="max-w-6xl mx-auto px-6 py-32 grid grid-cols-1 lg:grid-cols-2 gap-20 items-center border-t border-white/5">
        <div class="space-y-10 order-2 lg:order-1 text-left">
            <div class="space-y-4">
                <span class="text-accent text-[10px] font-black uppercase tracking-[0.3em]">Hvor & Hvornår</span>
                <h2 class="text-4xl md:text-5xl font-black italic tracking-tight text-white">Grønjordskollegiet</h2>
                <p class="text-dim text-lg leading-relaxed">
                    Vi samles i mit køkken/fælleslokale på Grønjordskollegiet til nogle timer med god mad og fejring. 
                    Det er uformelt, varmt og lige nøjagtig som det skal være.
                </p>
            </div>

            <div class="space-y-6">
                <div class="flex items-start gap-6 p-8 rounded-[32px] bg-white/[0.03] border border-glass-border shadow-sm group hover:border-accent/40 transition-all">
                    <div class="w-12 h-12 rounded-2xl bg-accent-soft flex items-center justify-center text-accent text-xl transition-transform group-hover:rotate-12">
                        <i class="fa-solid fa-map-location-dot"></i>
                    </div>
                    <div>
                        <h4 class="font-bold text-lg mb-1 text-white">Find Vej</h4>
                        <p class="text-dim text-sm mb-4">Blok X, Etage Y, Køkken Z. <br/> (Præcis lokation kommer tættere på)</p>
                        <a 
                            href="https://www.google.com/maps/search/?api=1&query=Grønjordskollegiet" 
                            target="_blank"
                            class="inline-flex items-center gap-2 text-[10px] font-black uppercase tracking-widest text-accent hover:text-white transition-colors"
                        >
                            <i class="fa-solid fa-location-arrow"></i> Åbn i Google Maps
                        </a>
                    </div>
                </div>

                <div class="flex items-start gap-6 p-8 rounded-[32px] bg-white/[0.03] border border-glass-border shadow-sm group hover:border-accent/40 transition-all">
                    <div class="w-12 h-12 rounded-2xl bg-accent-soft flex items-center justify-center text-accent text-xl transition-transform group-hover:rotate-12">
                        <i class="fa-solid fa-clock"></i>
                    </div>
                    <div>
                        <h4 class="font-bold text-lg mb-1 text-white">Tidspunkt</h4>
                        <p class="text-dim text-sm">Vi starter omkring kl. 14:00. <br/> (Placeholder tidspunkt)</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="aspect-[4/5] rounded-[64px] bg-white/5 overflow-hidden order-1 lg:order-2 relative group shadow-2xl">
            <div class="absolute inset-0 bg-accent/10 opacity-0 group-hover:opacity-100 transition-opacity duration-700"></div>
            <div class="w-full h-full flex items-center justify-center italic text-white/5 text-9xl">
                <i class="fa-solid fa-house-chimney"></i>
            </div>
            <div class="absolute bottom-10 left-10 p-8 bg-black/60 backdrop-blur-md rounded-[32px] border border-white/10 max-w-xs animate-in slide-in-from-bottom-4">
                <p class="text-accent font-black uppercase text-[10px] tracking-widest mb-2">Note fra kokken</p>
                <p class="text-sm font-medium italic text-white/90">"Jeg glæder mig til at vise jer mit hjem og servere noget af min yndlingsmad for jer."</p>
            </div>
        </div>
    </section>

    {/* The Menu Section */}
    <section class="bg-white/[0.02] py-40 border-y border-white/5 px-6">
        <div class="max-w-6xl mx-auto text-center space-y-24">
            <div class="space-y-4">
                <h2 class="text-5xl md:text-7xl font-black italic tracking-tighter text-white">Menuen</h2>
                <div class="w-12 h-1 bg-accent mx-auto rounded-full"></div>
                <p class="text-dim text-lg pt-4">En uformel servering af mine favoritter.</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-12 text-left">
                {menu.map((item, i) => (
                    <div class="group relative bg-white/[0.03] border border-glass-border rounded-[48px] overflow-hidden hover:border-accent/40 transition-all duration-500">
                        <div class="aspect-video relative overflow-hidden">
                            <img src={item.image} alt={item.title} class="w-full h-full object-cover transition-transform duration-1000 group-hover:scale-110 opacity-60 group-hover:opacity-100" />
                            <div class="absolute inset-0 bg-gradient-to-t from-[#0a0a0a] to-transparent"></div>
                            <div class="absolute top-6 right-6 flex gap-2">
                                {item.tags.map(tag => (
                                    <span class="px-3 py-1 bg-black/40 backdrop-blur-md border border-white/10 rounded-full text-[10px] font-black uppercase tracking-widest text-white/80">
                                        {tag}
                                    </span>
                                ))}
                            </div>
                        </div>
                        
                        <div class="p-10 space-y-4">
                            <div class="flex items-center gap-4">
                                <div class="w-12 h-12 rounded-2xl bg-accent-soft flex items-center justify-center text-accent text-xl transition-transform group-hover:rotate-12">
                                    <i class={`fa-solid ${item.icon}`}></i>
                                </div>
                                <h3 class="text-2xl font-black uppercase tracking-tight text-white">{item.title}</h3>
                            </div>
                            <p class="text-dim leading-relaxed italic">{item.description}</p>
                        </div>
                    </div>
                ))}
            </div>
        </div>
    </section>

    {/* FAQ for Family */}
    <section class="max-w-4xl mx-auto px-6 py-40 border-b border-white/5 space-y-24">
        <div class="text-center space-y-4">
            <span class="text-accent text-[10px] font-black uppercase tracking-[0.3em]">Spørgsmål & Svar</span>
            <h2 class="text-5xl font-black italic tracking-tighter text-white">Det praktiske</h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
            {[
                { q: "Hvor kan vi parkere?", a: "Der er parkeringspladser langs Grønjordsvej og ved kollegiet. Husk at tjekke skiltningen, da Amager kan være lidt striks med parkering!", icon: "fa-car" },
                { q: "Er der elevator?", a: "Ja, der er elevator i alle blokke, så det er nemt at komme op til mit køkken, også hvis man er dårligt gående.", icon: "fa-elevator" },
                { q: "Hvad med gaver?", a: "Det vigtigste er jeres selskab! Hvis I vil give noget, er et lille bidrag til min fremtidige opsparing altid værdsat.", icon: "fa-gift" },
                { q: "Andet logistik?", a: "Hvis I har spørgsmål eller bliver væk, kan I altid ringe direkte til mig. Jeg sender mit nummer ud i god tid.", icon: "fa-circle-info" }
            ].map(item => (
                <div class="p-10 rounded-[40px] bg-white/[0.03] border border-glass-border hover:bg-white/[0.05] transition-all group">
                    <div class="w-12 h-12 bg-accent/10 rounded-2x flex items-center justify-center text-accent mb-6 group-hover:scale-110 transition-transform">
                        <i class={`fa-solid ${item.icon}`}></i>
                    </div>
                    <h4 class="text-xl font-bold text-white mb-2 uppercase tracking-tight italic">{item.q}</h4>
                    <p class="text-dim text-sm leading-relaxed">{item.a}</p>
                </div>
            ))}
        </div>
    </section>

    {/* RSVP for Reception */}
    <section class="max-w-4xl mx-auto px-6 py-40 text-center space-y-16">
        <div class="space-y-4">
            <h2 class="text-5xl md:text-6xl font-black italic tracking-tighter text-white">Svar udbedes</h2>
            <p class="text-dim text-lg">Giv venligst besked om I kan komme til receptionen.</p>
        </div>

        <form class="max-w-xl mx-auto grid grid-cols-1 gap-6 text-left">
            <div class="space-y-2">
                <label class="text-[10px] font-black uppercase tracking-widest pl-4 text-dim">Navn</label>
                <input required type="text" placeholder="Navn..." class="w-full px-8 py-5 bg-white/5 border border-glass-border rounded-3xl outline-none focus:border-accent transition-all placeholder:text-white/10 text-white" />
            </div>
            <div class="space-y-2">
                <label class="text-[10px] font-black uppercase tracking-widest pl-4 text-dim">Antal Personer</label>
                <select class="w-full px-8 py-5 bg-white/5 border border-glass-border rounded-3xl outline-none focus:border-accent transition-all appearance-none cursor-pointer font-medium text-white">
                    <option value="1" class="bg-black">1 Person</option>
                    <option value="2" class="bg-black">2 Personer</option>
                    <option value="3" class="bg-black">3 Personer</option>
                    <option value="4" class="bg-black">4 Personer</option>
                </select>
            </div>
            <div class="space-y-2">
                <label class="text-[10px] font-black uppercase tracking-widest pl-4 text-dim">Eventuelle Allergier</label>
                <textarea rows="3" placeholder="Skriv her..." class="w-full px-8 py-5 bg-white/5 border border-glass-border rounded-3xl outline-none focus:border-accent transition-all resize-none placeholder:text-white/10 text-white"></textarea>
            </div>
            <button type="submit" class="w-full py-6 bg-white text-black font-black rounded-3xl hover:bg-accent transition-all shadow-xl active:scale-[0.98]">
                Send Bekræftelse <i class="fa-solid fa-paper-plane ml-3 text-xs"></i>
            </button>
            <p class="text-[10px] text-dim/30 font-bold uppercase tracking-widest text-center pt-4">
                (Dette er en placeholder formular)
            </p>
        </form>
    </section>

    {/* Footer */}
    <footer class="py-20 text-center border-t border-white/5">
        <p class="text-dim text-sm font-medium">Anton Ebsen — 2026 — Grønjordskollegiet</p>
    </footer>

  </main>
</Layout>

<style>
  .text-glow-accent { text-shadow: 0 0 20px rgba(212, 175, 55, 0.4); }
  .outline-text-accent { -webkit-text-stroke: 1px rgba(212, 175, 55, 0.2); }
  
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .shadow-sm { box-shadow: 0 10px 40px -10px rgba(0,0,0,0.05); }
  .shadow-md { box-shadow: 0 20px 60px -15px rgba(0,0,0,0.1); }
</style>
