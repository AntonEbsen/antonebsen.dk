---
import Layout from '@layouts/BaseLayout.astro';
import wishes from '../data/wishes.json';

// Password protection check (same as graduation.astro)
export const prerender = false;
let authenticated = true; // We assume they come from the graduation page which is already gated
---

<Layout title="Ã˜nskeseddel | Anton Ebsen" description="Mine Ã¸nsker til dimissionsfesten">
  <main id="wishes-content" class="min-h-screen bg-black text-white px-4 py-20 relative overflow-hidden flex flex-col items-center">
    
    {/* Canvas for Mouse Trail */}
    <canvas id="particle-canvas" class="fixed inset-0 pointer-events-none z-[1]" />

    {/* Background Glows */}
    <div class="absolute top-0 left-1/4 w-[500px] h-[500px] bg-accent/5 blur-[120px] rounded-full pointer-events-none -z-10"></div>
    <div class="absolute bottom-0 right-1/4 w-[500px] h-[500px] bg-accent/5 blur-[120px] rounded-full pointer-events-none -z-10"></div>

    {/* Header Section */}
    <div class="max-w-4xl w-full text-center space-y-8 mb-20 relative z-10">
        <div class="flex flex-col sm:flex-row items-center justify-between gap-6 mb-12">
            <a href="/graduation" class="inline-flex items-center gap-2 text-[10px] font-black uppercase tracking-[0.3em] text-dim hover:text-accent transition-colors group">
                <i class="fa-solid fa-arrow-left group-hover:-translate-x-1 transition-transform"></i> Tilbage til invitationen
            </a>
            
            <button 
                id="copy-link-btn"
                class="inline-flex items-center gap-3 px-6 py-3 bg-white/5 border border-white/10 rounded-full text-[10px] font-black uppercase tracking-widest text-dim hover:text-white hover:border-accent/40 transition-all group"
            >
                <i class="fa-solid fa-link group-hover:rotate-12 transition-transform"></i> Del Ã¸nskeseddel
            </button>
        </div>
        
        <div class="space-y-4">
            <div class="w-20 h-20 bg-accent-soft rounded-3xl border border-accent/20 flex items-center justify-center mx-auto mb-8 shadow-glow">
                <i class="fa-solid fa-gift text-accent text-4xl"></i>
            </div>
            <h1 class="text-5xl md:text-7xl font-black italic tracking-tighter">
                Min <span class="text-accent text-glow-accent outline-text-accent">Ã˜nskeseddel</span>
            </h1>
            <p class="text-dim text-lg max-w-xl mx-auto leading-relaxed">
                Her er en oversigt over hvad jeg drÃ¸mmer om til min dimission. 
                Du kan se den fulde og opdaterede liste direkte pÃ¥ Ã˜nskeskyen.
            </p>
        </div>

        {/* Primary CTA */}
        <div class="pt-8">
            <a 
                href="https://onskeskyen.dk/da/wishlists/GyqRcxg5dRdV5GyU" 
                target="_blank"
                class="inline-flex items-center gap-4 px-12 py-6 bg-white text-black font-black rounded-full hover:bg-accent hover:scale-105 transition-all shadow-[0_20px_60px_-15px_rgba(255,255,255,0.3)] group duration-500"
            >
                Se hele listen pÃ¥ Ã˜nskeskyen <i class="fa-solid fa-cloud group-hover:translate-y-[-2px] transition-transform"></i>
            </a>
        </div>
    </div>

    {/* MobilePay "Quick-Gift" (New) */}
    <section class="max-w-4xl w-full mb-20 relative z-10">
        <div class="p-8 md:p-12 rounded-[56px] bg-gradient-to-br from-accent/10 to-transparent border border-accent/20 flex flex-col md:flex-row items-center justify-between gap-8 group hover:border-accent/40 transition-all duration-500 backdrop-blur-sm">
            <div class="text-center md:text-left space-y-2">
                <div class="inline-flex items-center gap-2 text-accent text-[10px] font-black uppercase tracking-widest mb-2">
                    <i class="fa-solid fa-bolt"></i> Quick-Gift
                </div>
                <h3 class="text-3xl font-bold italic">Et bidrag til de nÃ¦ste 5,5 Ã¥r?</h3>
                <p class="text-dim text-sm max-w-sm">Hvis du hellere vil give et bidrag til mine fremtidige eventyr og rejser, er en skilling til MobilePay altid velkommen.</p>
            </div>
            
            <div class="flex flex-col items-center gap-4">
                <button 
                    id="copy-mobilepay-btn"
                    class="px-8 py-5 bg-[#0055ff]/10 border border-[#0055ff]/20 rounded-3xl flex items-center gap-4 group-hover:bg-[#0055ff]/20 transition-all cursor-pointer active:scale-95"
                >
                    <div class="w-12 h-12 bg-[#0055ff] rounded-xl flex items-center justify-center text-white text-2xl shadow-lg">
                        <i class="fa-solid fa-mobile-screen-button"></i>
                    </div>
                    <div class="text-left">
                        <p class="text-[10px] text-dim font-bold uppercase tracking-widest leading-none mb-1">MobilePay</p>
                        <p id="mobilepay-number" class="text-xl font-black text-white">+45 XX XX XX XX</p>
                    </div>
                </button>
                <p class="text-[10px] text-dim/40 italic uppercase tracking-widest">Klik for at kopiere nummer</p>
            </div>
        </div>
    </section>

    {/* Wishes Grid */}
    <section class="max-w-6xl w-full grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-40 relative z-10">
        {wishes.map((wish, i) => (
            <div 
                class="p-10 rounded-[48px] bg-white/[0.03] border border-glass-border hover:bg-white/[0.06] hover:border-accent/40 transition-all duration-500 group relative overflow-hidden"
                style={`animation: slideUp 0.8s ease-out ${i * 0.1}s both`}
            >
                <div class="absolute inset-0 bg-gradient-to-br from-accent/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
                
                <div class="relative z-10 space-y-6">
                    <div class="w-16 h-16 bg-white/5 rounded-2xl flex items-center justify-center text-accent/40 group-hover:text-accent transition-colors border border-white/5 group-hover:border-accent/20 transform group-hover:rotate-12 duration-500">
                        <i class={`fa-solid ${wish.icon} text-3xl`}></i>
                    </div>
                    
                    <div class="space-y-2">
                        <h3 class="text-2xl font-bold uppercase tracking-tight">{wish.title}</h3>
                        <p class="text-dim text-sm leading-relaxed">{wish.description}</p>
                    </div>

                    <div class="pt-4">
                        <div class="h-0.5 w-12 bg-white/10 group-hover:w-full group-hover:bg-accent transition-all duration-700"></div>
                    </div>
                </div>
            </div>
        ))}
    </section>

    {/* Footer Callout */}
    <div class="max-w-2xl w-full text-center p-12 rounded-[56px] border border-accent/20 bg-accent/5 backdrop-blur-xl relative overflow-hidden mb-20 z-10">
        <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_right,rgba(212,175,55,0.1),transparent)]"></div>
        <p class="text-white/80 font-bold italic text-xl mb-4 relative z-10">
            "Den stÃ¸rste gave er din tilstedevÃ¦relse til min fest!"
        </p>
        <p class="text-dim text-sm relative z-10">
            Jeg glÃ¦der mig helt vildt til at se dig d. 1. juni.
        </p>
    </div>

  </main>
</Layout>

<script>
  import confetti from 'canvas-confetti';

  const wishesContent = document.getElementById('wishes-content');
  if (wishesContent) {
    // 1. Mouse Trail Particles (Copied from main page)
    const canvas = document.getElementById('particle-canvas') as HTMLCanvasElement;
    if (canvas) {
        const ctx = canvas.getContext('2d')!;
        let particles: any[] = [];
        const resize = () => { canvas.width = window.innerWidth; canvas.height = window.innerHeight; };
        window.addEventListener('resize', resize); resize();
        class Particle {
            x: number; y: number; size: number; speedX: number; speedY: number; life: number;
            constructor(x: number, y: number) {
                this.x = x; this.y = y; this.size = Math.random() * 2 + 0.5;
                this.speedX = (Math.random() - 0.5) * 1.5; this.speedY = (Math.random() - 0.5) * 1.5;
                this.life = 1;
            }
            update() { this.x += this.speedX; this.y += this.speedY; this.life -= 0.015; }
            draw() { ctx.fillStyle = `rgba(212, 175, 55, ${this.life})`; ctx.beginPath(); ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2); ctx.fill(); }
        }
        window.addEventListener('mousemove', (e) => { for (let i = 0; i < 2; i++) particles.push(new Particle(e.clientX, e.clientY)); });
        const animate = () => {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            particles = particles.filter(p => p.life > 0);
            particles.forEach(p => { p.update(); p.draw(); });
            requestAnimationFrame(animate);
        };
        animate();
    }

    // 2. Copy functionality
    const copyBtn = document.getElementById('copy-link-btn');
    if (copyBtn) {
        copyBtn.addEventListener('click', () => {
            navigator.clipboard.writeText(window.location.href);
            const originalText = copyBtn.innerHTML;
            copyBtn.innerHTML = '<i class="fa-solid fa-check text-accent"></i> Kopieret!';
            confetti({ particleCount: 20, spread: 30, origin: { y: 0.1 }, colors: ['#d4af37'] });
            setTimeout(() => { copyBtn.innerHTML = originalText; }, 2000);
        });
    }
    // 3. MobilePay Copy
    const mpBtn = document.getElementById('copy-mobilepay-btn');
    const mpNum = document.getElementById('mobilepay-number');
    if (mpBtn && mpNum) {
        mpBtn.addEventListener('click', () => {
            navigator.clipboard.writeText(mpNum.innerText);
            confetti({ particleCount: 30, spread: 50, origin: { y: 0.6 }, colors: ['#0055ff', '#ffffff'] });
            // Visual feedback
            const originalColor = mpBtn.style.borderColor;
            mpBtn.style.borderColor = '#0055ff';
            setTimeout(() => { mpBtn.style.borderColor = originalColor; }, 1000);
        });
    }
  }
</script>

<style>
  .shadow-glow { box-shadow: 0 0 60px rgba(212, 175, 55, 0.2); }
  .text-glow-accent { text-shadow: 0 0 20px rgba(212, 175, 55, 0.5); }
  .outline-text-accent { -webkit-text-stroke: 1px rgba(212, 175, 55, 0.2); }
  
  @keyframes slideUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* Custom scrollbar for a premium feel */
  ::-webkit-scrollbar { width: 8px; }
  ::-webkit-scrollbar-track { background: #000; }
  ::-webkit-scrollbar-thumb { background: #333; border-radius: 4px; }
  ::-webkit-scrollbar-thumb:hover { background: #d4af37; }
</style>



