---
import MediaPage from '@components/templates/MediaPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<MediaPage lang={lang} />
