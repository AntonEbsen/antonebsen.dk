---
import Layout from '@layouts/Layout.astro';
---

<Layout 
  title="Anton Meier Ebsen Jørgensen – Publikationer & Noter" 
  description="Publikationer, working notes og korte one-pagers om økonomi, data og modeller."
  bodyClass="notes-page"
>
  <style>
    .notes-wrap {
      max-width: 1200px;
      margin: 24px auto;
      padding: 0 16px 32px;
    }
    
    .hero-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.6fr) minmax(0, 1fr);
      gap: 22px;
      align-items: start;
    }

    .notes-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 14px;
      margin-top: 12px;
    }

    @media (max-width: 980px) {
      .hero-grid, .notes-grid { 
        grid-template-columns: 1fr; 
      }
    }
  </style>

  <main class="notes-wrap">

    <section class="mb-12">
      <div class="hero-grid">
        <div class="bg-glass border border-glass-border p-8 rounded-2xl">
          <p class="text-xs font-bold uppercase tracking-widest text-accent mb-3">Publikationer / Noter</p>
          <h1 class="text-4xl font-bold leading-tight mb-4">Korte writeups og materiale</h1>
          <p class="text-lg text-dim">
            Her samler jeg ting, der er mere “arkiv og reference” end blog: one-pagers, slides og arbejdsnoter.
          </p>
        </div>
      </div>
    </section>

    <section id="noter" class="mb-20">
      <h2 class="text-xl font-bold mb-6">Noter</h2>
      <div class="notes-grid">
        <article class="bg-glass border border-glass-border p-6 rounded-xl">
           <h3 class="font-bold mb-2">Template: Data → Leverance</h3>
           <p class="text-sm text-dim mb-4">En enkel struktur jeg bruger til analyseprojekter.</p>
           <a href="#" class="text-accent font-bold">Læs note →</a>
        </article>
      </div>
    </section>

  </main>
</Layout>
