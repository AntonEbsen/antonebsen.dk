---
export const prerender = true;
import QAPage from '@components/templates/QAPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<QAPage lang={lang} />
