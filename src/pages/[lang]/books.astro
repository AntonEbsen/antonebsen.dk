---
export const prerender = true;
import BooksPage from '@components/templates/BooksPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<BooksPage lang={lang} />
