---
import Layout from '@layouts/Layout.astro';
import KPIGrid from '@components/stats/KPIGrid.astro';
import SessionTable from '@components/stats/SessionTable.astro';

const kpiData = [
  { title: "Antal sessions", value: "6", hint: "PUSH/PULL/LEGS x 2" },
  { title: "Øvelser logget", value: "—", hint: "Udfyld når du vil" },
  { title: "Total reps", value: "—", hint: "Reps på tværs af øvelser" },
  { title: "Total tonnage", value: "—", hint: "Σ (kg × reps)" }
];

const sessionData = [
  { date: "04-12-2025", day: "PUSH 1", exercise: "Incline Press", sets: 4, weight: 60, reps: "10/8/7/6" },
  { date: "05-12-2025", day: "PULL 1", exercise: "Deadlift", sets: 3, weight: "100/100/90", reps: "5/6/8" },
  { date: "06-12-2025", day: "LEGS 1", exercise: "Squat", sets: 4, weight: 100, reps: "8/8/7/6", note: "10 min pause (svimmel/kvalme)" },
  { date: "10-12-2025", day: "LEGS 2", exercise: "Leg Press", sets: 4, weight: 147, reps: "10/9/9/8" }
];
---

<Layout 
  title="Anton Meier Ebsen Jørgensen – Træningsstatistik" 
  description="Træningsstatistik: nøgletal, volumen, top-sæt og progression. Opdateres løbende."
  bodyClass="training-page"
>
<style>
.training-wrap {
  max-width: 1200px;
  margin: 24px auto;
  padding: 0 16px 32px;
}

/* Cards */
.card {
  background: var(--glass);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-radius: var(--radius);
  padding: 24px;
  border: 1px solid var(--glass-border);
  box-shadow: var(--shadow);
  transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), border-color 0.3s ease;
}
.card:hover {
  transform: translateY(-4px) scale(1.01);
  border-color: var(--accent-soft);
}

.section-title {
  margin: 0 0 24px;
  font-size: 13px;
  text-transform: uppercase;
  letter-spacing: 0.2em;
  font-weight: 800;
  color: var(--accent);
}

.eyebrow {
  margin: 0 0 12px;
  color: var(--text-dim);
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  font-size: 12px;
}

.lead { 
  margin: 0 0 24px; 
  color: var(--text-dim); 
  font-size: 18px; 
  line-height: 1.6; 
}

.muted { 
  color: var(--text-dim); 
  opacity: 0.7; 
}

/* Buttons */
.cta {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin: 10px 0 0;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 12px 24px;
  border-radius: 999px;
  background: var(--accent);
  color: #050506;
  text-decoration: none;
  font-weight: 700;
  font-size: 14.5px;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.btn:hover {
  transform: translateY(-2px);
  box-shadow: var(--glow);
}

.btn.ghost {
  background: transparent;
  border: 1.5px solid var(--accent);
  color: var(--accent);
}

/* Hero layout */
.hero {
  display: grid;
  grid-template-columns: minmax(0, 1.6fr) minmax(0, 1fr);
  gap: 22px;
  align-items: start;
  margin-bottom: 14px;
}

.mini-card {
  background: var(--glass);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-radius: 14px;
  padding: 16px;
  border: 1px solid var(--glass-border);
  transition: border-color 0.3s ease;
}

.mini-card:hover { 
  border-color: var(--accent-soft); 
}

/* Split grid */
.grid-2 {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
}

/* Pills */
.pill-wrap {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 10px;
}

.pill {
  padding: 6px 14px;
  background: var(--accent-soft);
  border: 1px solid var(--accent);
  border-radius: 999px;
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--accent);
}

/* Mobile */
@media (max-width: 980px) {
  .hero { 
    grid-template-columns: 1fr; 
  }
  .grid-2 { 
    grid-template-columns: 1fr; 
  }
}
</style>

  <main class="training-wrap">

    <!-- HERO -->
    <section class="card hero">
      <div>
        <p class="eyebrow">Træning</p>
        <h1>Træningsstatistik</h1>
        <p class="lead">
          Nøgletal for mine sessions (PUSH/PULL/LEGS): volumen, top-sæt og estimeret styrke.
          Programmet er stabilt, så udvikling og plateaus er lette at spotte.
        </p>

        <div class="pill-wrap" aria-label="Fokusområder">
          <span class="pill">Volumen (tonnage)</span>
          <span class="pill">Top-sæt</span>
          <span class="pill">Est. 1RM</span>
          <span class="pill">Plateau-watch</span>
        </div>

        <div class="cta">
          <a class="btn" href="#sessions"><i class="fa-solid fa-table"></i> Se sessions</a>
          <a class="btn ghost" href="#method"><i class="fa-solid fa-circle-info"></i> Metode</a>
          <a class="btn ghost" href="/blog#cat-traening"><i class="fa-solid fa-arrow-left"></i> Tilbage til blog</a>
        </div>
      </div>

      <div class="hero-side">
        <div class="mini-card">
          <h2 class="section-title">Status</h2>
          <p class="muted small" style="margin:0;">
            Data: foreløbig 1 uge. Når du tilføjer flere uger, kan vi automatisk vise trends pr. øvelse og pr. muskelgruppe.
          </p>
          <p class="muted small" style="margin-top:10px;">
            Tip: hold øvelsesnavne stabile (samme stavning), så statistikken ikke splitter sig.
          </p>
        </div>
      </div>
    </section>

    <!-- KPI -->
    <section class="card" aria-label="Nøgletal">
      <h2 class="section-title">Nøgletal (seneste uge)</h2>

      <KPIGrid items={kpiData} />

      <p class="muted small" style="margin:0;">
        *KPI’er bliver først “rigtigt fede” når der er 4+ uger, så man kan se gennemsnit, variation og trend.
      </p>
    </section>

    <!-- SPLIT OVERVIEW -->
    <section class="grid-2" style="margin-top:14px;">
      <section class="card" aria-label="Split overblik">
        <h2 class="section-title">Split</h2>
        <p class="muted" style="margin-top:0;">
          PUSH / PULL / LEGS. Du kører til failure, så vi kan bruge top-sæt som “performance proxy”.
        </p>
        <ul class="muted" style="margin:0; padding-left:18px;">
          <li><strong>Progression:</strong> reps ↑ ved samme vægt, eller vægt ↑ ved samme reps</li>
          <li><strong>Plateau:</strong> 2–3 uger uden forbedring på top-sæt</li>
          <li><strong>Deload:</strong> sænk volumen 30–50% hvis CNS/led føles cooked</li>
        </ul>
      </section>

      <section class="card" id="method" aria-label="Metode">
        <h2 class="section-title">Metode (kort)</h2>
        <p class="muted" style="margin-top:0;">
          Jeg tracker de vigtigste ting – ikke alt. Fokus er at kunne sammenligne uge-for-uge.
        </p>
        <ul class="muted" style="margin:0; padding-left:18px;">
          <li><strong>Tonnage:</strong> Σ(kg × reps) pr. øvelse + pr. dag</li>
          <li><strong>Top-sæt:</strong> bedste sæt pr. øvelse</li>
          <li><strong>Est. 1RM:</strong> fx Epley: 1RM ≈ kg × (1 + reps/30)</li>
          <li><strong>Noter:</strong> pauser, kvalme, udstyr, “what happened today??”</li>
        </ul>
      </section>
    </section>

    <!-- SESSIONS TABLE -->
    <section class="card" id="sessions" style="margin-top:14px;">
      <h2 class="section-title">Sessions (råt overblik)</h2>
      <p class="muted">Her kan du løbende tilføje nye rækker. Tabellen scroller vandret på mobil.</p>

      <SessionTable sessions={sessionData} />

      <div class="cta" style="margin-top:14px;">
        <a class="btn ghost" href="#method"><i class="fa-solid fa-arrow-up"></i> Top</a>
        <a class="btn" href="/contact"><i class="fa-solid fa-envelope"></i> Kontakt</a>
      </div>
    </section>

    <!-- NEXT STEP -->
    <section class="card" style="margin-top:14px;">
      <h2 class="section-title">Næste skridt (når du har 4+ uger)</h2>
      <p class="muted" style="margin-top:0;">
        Så kan vi gøre siden meget mere “stats-agtig”:
      </p>
      <ul class="muted" style="margin:0; padding-left:18px;">
        <li>Top-sæt trend pr. øvelse (fx Incline Press: bedste sæt uge for uge)</li>
        <li>Volumen pr. muskelgruppe (bryst/ryg/ben/skuldre/arme)</li>
        <li>Plateau-detektion: marker hvis top-sæt ikke forbedres i 2–3 uger</li>
        <li>Auto-genereret ugeopsummering (med highlights og “watchouts”)</li>
      </ul>
    </section>

  </main>
</Layout>
