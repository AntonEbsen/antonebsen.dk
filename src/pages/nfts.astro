---
import NFTPage from '@components/templates/NFTPage.astro';
import { getEntry } from 'astro:content';

export const prerender = true;

const entry = await getEntry('pages', 'da/nfts');
if (!entry) throw new Error('Could not find content for da/nfts');
---

<NFTPage lang="da" content={entry.data} />
