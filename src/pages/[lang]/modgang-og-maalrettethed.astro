---
export const prerender = true;
import AdversityPage from '@components/templates/AdversityPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<AdversityPage lang={lang} />
