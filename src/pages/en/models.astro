---
import ModelsPage from '@components/templates/ModelsPage.astro';
import { getEntry } from 'astro:content';

export const prerender = true;

const entry = await getEntry('pages', 'en/models');
if (!entry) throw new Error('Could not find content for en/models');
---

<ModelsPage lang="en" content={entry.data} />
