---
export const prerender = true;
import ExperiencePage from '@components/templates/ExperiencePage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<ExperiencePage lang={lang} />
