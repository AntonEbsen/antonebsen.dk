---
import CoursesPage from '@components/templates/CoursesPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<CoursesPage lang={lang} />
