---
export const prerender = true;
import PortfolioPage from '@components/templates/PortfolioPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<PortfolioPage lang={lang} />
