---
import TrainingProgramPage from '@components/templates/TrainingProgramPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<TrainingProgramPage lang={lang} />
