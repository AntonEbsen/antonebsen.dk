---
export const prerender = true;
import BlogPage from '@components/templates/BlogPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<BlogPage lang={lang} />
