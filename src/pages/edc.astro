---
import EDCPage from '@components/templates/EDCPage.astro';
import { getEntry } from 'astro:content';

export const prerender = true;

const entry = await getEntry('pages', 'da/edc');
if (!entry) throw new Error('Could not find content for da/edc');
---

<EDCPage lang="da" content={entry.data} />
