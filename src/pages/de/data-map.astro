---
import DataMapPage from '@components/templates/DataMapPage.astro';
import { getEntry } from 'astro:content';

export const prerender = true;

const entry = await getEntry('pages', 'de/data-map');
if (!entry) throw new Error('Could not find content for de/data-map');
---

<DataMapPage lang="de" content={entry.data} />
