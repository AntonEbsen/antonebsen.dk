---
import Layout from '../layouts/Layout.astro';
import Section from '../components/ui/Section.astro';
import Card from '../components/ui/Card.astro';
import Button from '../components/ui/Button.astro';
import Pill from '../components/ui/Pill.astro';
---

<Layout 
  title="Anton Meier Ebsen Jørgensen – Ressourcer & Downloads" 
  description="Gratis ressourcer og downloads: Excel-skabeloner, kode, noter og guides."
  bodyClass="resources-page"
>
  <style>
    .resources-wrap {
      max-width: 1200px;
      margin: 24px auto;
      padding: 0 16px 32px;
    }

    .hero {
      display: grid;
      grid-template-columns: minmax(0, 1.6fr) minmax(0, 1fr);
      gap: 22px;
      align-items: start;
    }

    .dl-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0,1fr));
      gap: 14px;
      margin-top: 12px;
    }

    @media (max-width: 980px) {
      .hero, .dl-grid { 
        grid-template-columns: 1fr; 
      }
    }
  </style>

  <main class="resources-wrap">

    <!-- HERO -->
    <Section class="hero">
      <Card variant="glass" class="p-6">
        <p class="text-xs font-extrabold uppercase tracking-wider text-[var(--text-dim)] mb-3">Ressourcer</p>
        <h1 class="text-[clamp(26px,4vw,40px)] leading-tight mb-4 font-bold">Downloads & templates</h1>
        <p class="text-lg leading-relaxed text-[var(--text-dim)] mb-6">
          Små, praktiske ressourcer jeg selv bruger: skabeloner, cheat sheets og eksempler.
          Alt er lavet til at være plug-and-play.
        </p>

        <div class="flex flex-wrap gap-2 mb-6">
          <Pill variant="accent">Excel</Pill>
          <Pill variant="accent">VBA</Pill>
          <Pill variant="accent">Python</Pill>
          <Pill variant="accent">SAS</Pill>
          <Pill variant="accent">GAMS</Pill>
        </div>

        <div class="flex flex-wrap gap-3">
          <Button href="#downloads">Se downloads</Button>
          <Button href="/kursusstatistik" variant="ghost">
            <i class="fa-solid fa-chart-column mr-2"></i> Se kursusstatistik
          </Button>
          <Button href="/contact" variant="ghost">Spørg om noget specifikt</Button>
        </div>
      </Card>

      <div class="hidden md:block">
        <Card variant="glass" class="p-4">
          <h2 class="text-xs font-bold uppercase tracking-wider text-[var(--accent)] mb-4">Note</h2>
          <p class="text-[13px] text-[var(--text-dim)] mb-4 opacity-70">
            Nogle filer kan være “sanitized” (fjernet data/navne) pga. kursus/fortrolighed.
          </p>
          <p class="text-[13px] text-[var(--text-dim)] opacity-70">
            Har du et konkret behov? Skriv — jeg kan ofte lave en skabelon, der passer.
          </p>
        </Card>
      </div>
    </Section>

    <!-- DOWNLOADS -->
    <Section id="downloads">
      <Card variant="bordered" class="bg-[rgba(255,255,255,0.01)]!">
        <h2 class="text-xs font-bold uppercase tracking-wider text-[var(--accent)] mb-2">Downloads</h2>
        <p class="text-[var(--text-dim)] opacity-70 mb-6">Placér filer i mappen <code>assets/downloads/</code> og link dem her.</p>

        <div class="dl-grid">

          <!-- NEW: Kursusstatistik -->
          <Card variant="bordered" class="flex flex-col h-full bg-[rgba(17,24,39,0.02)]!">
            <div class="flex justify-between items-start mb-2 gap-2">
              <h3 class="text-[16px] font-bold leading-tight m-0">Kursusstatistik: evalueringer & nøgletal</h3>
              <Pill variant="accent" class="shrink-0">Undervisning</Pill>
            </div>
            <p class="text-[13px] text-[var(--text-dim)] mb-3 opacity-90">Webside · opdateret: (indsæt dato)</p>
            <p class="text-[14px] text-[var(--text-dim)] flex-grow mb-4 leading-relaxed opacity-90">
              Overblik over kursusevalueringer og statistik på tværs af hold. Kort, konkret og let at skimme.
            </p>
            <div class="flex flex-wrap gap-2 mt-auto">
              <Button href="/kursusstatistik" class="px-4 py-2 text-xs">
                <i class="fa-solid fa-arrow-up-right-from-square mr-2"></i> Se side
              </Button>
              <Button href="/assets/downloads/kursusevalueringer-statistik.xlsx" download variant="ghost" class="px-4 py-2 text-xs">
                <i class="fa-solid fa-download mr-2"></i> Download data
              </Button>
            </div>
          </Card>

          <!-- Download card 1 -->
          <Card variant="bordered" class="flex flex-col h-full bg-[rgba(17,24,39,0.02)]!">
            <div class="flex justify-between items-start mb-2 gap-2">
              <h3 class="text-[16px] font-bold leading-tight m-0">Excel: Budget & model-skabelon</h3>
              <Pill variant="accent" class="shrink-0">Excel</Pill>
            </div>
            <p class="text-[13px] text-[var(--text-dim)] mb-3 opacity-90">Template · opdateret: (indsæt dato)</p>
            <p class="text-[14px] text-[var(--text-dim)] flex-grow mb-4 leading-relaxed opacity-90">
              En ren skabelon til struktur: inputs, beregninger, output, checks og scenarier.
            </p>
            <div class="flex flex-wrap gap-2 mt-auto">
              <Button href="/assets/downloads/excel-template.xlsx" download class="px-4 py-2 text-xs">
                <i class="fa-solid fa-download mr-2"></i> Download
              </Button>
              <Button href="#" variant="ghost" class="px-4 py-2 text-xs opacity-50 cursor-not-allowed">Beskrivelse</Button>
            </div>
          </Card>

          <!-- Download card 2 -->
          <Card variant="bordered" class="flex flex-col h-full bg-[rgba(17,24,39,0.02)]!">
            <div class="flex justify-between items-start mb-2 gap-2">
              <h3 class="text-[16px] font-bold leading-tight m-0">VBA: Snippets (hurtige genveje)</h3>
              <Pill variant="accent" class="shrink-0">VBA</Pill>
            </div>
            <p class="text-[13px] text-[var(--text-dim)] mb-3 opacity-90">ZIP/tekst · opdateret: (indsæt dato)</p>
            <p class="text-[14px] text-[var(--text-dim)] flex-grow mb-4 leading-relaxed opacity-90">
              Korte code blocks: import/cleaning, formatting, loops, performance tips.
            </p>
            <div class="flex flex-wrap gap-2 mt-auto">
              <Button href="/assets/downloads/vba-snippets.zip" download class="px-4 py-2 text-xs">
                <i class="fa-solid fa-download mr-2"></i> Download
              </Button>
              <Button href="https://github.com/AntonEbsen" target="_blank" rel="noopener" variant="ghost" class="px-4 py-2 text-xs">
                <i class="fa-brands fa-github mr-2"></i> GitHub
              </Button>
            </div>
          </Card>

          <!-- Download card 3 -->
          <Card variant="bordered" class="flex flex-col h-full bg-[rgba(17,24,39,0.02)]!">
            <div class="flex justify-between items-start mb-2 gap-2">
              <h3 class="text-[16px] font-bold leading-tight m-0">Python: Data-cleaning checklist</h3>
              <Pill variant="accent" class="shrink-0">Python</Pill>
            </div>
            <p class="text-[13px] text-[var(--text-dim)] mb-3 opacity-90">PDF · opdateret: (indsæt dato)</p>
            <p class="text-[14px] text-[var(--text-dim)] flex-grow mb-4 leading-relaxed opacity-90">
              En praktisk tjekliste til reproducérbar data prep (missing, outliers, splits, logs).
            </p>
            <div class="flex flex-wrap gap-2 mt-auto">
              <Button href="/assets/downloads/python-checklist.pdf" download class="px-4 py-2 text-xs">
                <i class="fa-solid fa-download mr-2"></i> Download
              </Button>
              <Button href="#" variant="ghost" class="px-4 py-2 text-xs opacity-50 cursor-not-allowed">Se eksempel</Button>
            </div>
          </Card>

        </div>
      </Card>
    </Section>

    <!-- CTA -->
    <Section>
      <Card variant="default">
        <h2 class="text-xs font-bold uppercase tracking-wider text-[var(--accent)] mb-3">Vil du have noget specifikt?</h2>
        <p class="text-[var(--text-dim)] opacity-70 mb-6">
          Hvis du fortæller mig dit format (Excel/Python/SAS) og mål (fx forecasting, dashboards, data-cleaning),
          kan jeg foreslå en template-struktur, der passer.
        </p>
        <div class="flex flex-wrap gap-3">
          <Button href="/contact">Kontakt</Button>
          <Button href="/services" variant="ghost">Se services</Button>
        </div>
      </Card>
    </Section>

  </main>
</Layout>
