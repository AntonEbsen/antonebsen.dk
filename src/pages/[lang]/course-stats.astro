---
export const prerender = true;
import CourseStatsPage from '@components/templates/CourseStatsPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<CourseStatsPage lang={lang} />
