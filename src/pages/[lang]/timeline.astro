---
import TimelinePage from '@components/templates/TimelinePage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<TimelinePage lang={lang} />
