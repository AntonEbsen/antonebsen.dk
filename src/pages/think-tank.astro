---
import ThinkTankPage from '@components/templates/ThinkTankPage.astro';
import { getEntry } from 'astro:content';

export const prerender = true;

const entry = await getEntry('pages', 'da/think-tank');
if (!entry) throw new Error('Could not find content for da/think-tank');
---

<ThinkTankPage lang="da" content={entry.data} />
