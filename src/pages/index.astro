---
import Layout from '../layouts/Layout.astro';
import Pill from '../components/common/Pill.astro';
import Section from '../components/common/Section.astro';
import Card from '../components/common/Card.astro';
import homeData from '../data/home.json';
import projectsData from '../data/projects.json';
---

<Layout 
  title="Anton Meier Ebsen Jørgensen – Økonomi, makro & data" 
  description="Økonomistuderende (cand.polit.) med fokus på makroøkonomi, økonometri og økonomiske modeller. Erfaring med Python, SAS, GAMS og Excel/VBA."
  bodyClass="home-page"
>


  <main class="home-wrap">
    <!-- HERO -->
    <Section class="card hero">
      <div class="hero-left">
        <p class="eyebrow">{homeData.hero.eyebrow}</p>
        <h1>{homeData.hero.title}</h1>
        <p class="lead">
          {homeData.hero.lead}
        </p>

        <div class="cta">
          <a class="btn" href="/cv">Se CV</a>
          <a class="btn ghost" href="/portfolio">Se portefølje</a>
          <a class="btn ghost" href="/assets/Anton-CV.pdf" target="_blank" rel="noopener">Download PDF</a>
        </div>

        <div class="pill-wrap">
          {homeData.hero.pills.map(pill => (
            <Pill text={pill} />
          ))}
        </div>
      </div>

      <div class="hero-right">
        <img src="/assets/img/billede-af-mig.jpg" alt="Anton Meier Ebsen Jørgensen" class="hero-img">
        <div class="hero-links">
          <a href="mailto:anton.ebsen@proton.me"><i class="fa-solid fa-envelope"></i> Email</a>
          <a href="https://www.linkedin.com/in/antonebsen/" target="_blank" rel="noopener"><i class="fa-brands fa-linkedin"></i> LinkedIn</a>
          <a href="https://github.com/AntonEbsen" target="_blank" rel="noopener"><i class="fa-brands fa-github"></i> GitHub</a>
        </div>
      </div>
    </Section>

    <!-- HIGHLIGHTS -->
    <section class="grid-3">
      <Card>
        <h2 class="section-title">{homeData.work.title}</h2>
        <ul class="clean-list">
          {homeData.work.items.map(item => (
            <li>{item}</li>
          ))}
        </ul>
      </Card>

      <Card>
        <h2 class="section-title">{homeData.projects.title}</h2>
        <ul class="clean-list">
          {projectsData.map(project => (
            <li><strong>{project.title.split(": ")[0]}:</strong> {project.title.split(": ")[1]}</li>
          ))}
        </ul>
        <p class="muted" style="margin-top:10px;">
          <a href="/portfolio">Se mere i porteføljen →</a>
        </p>
      </Card>

      <Card>
        <h2 class="section-title">Hurtig adgang</h2>
        <div class="quick-links">
          <a class="btn ghost" href="/cv">CV</a>
          <a class="btn ghost" href="/portfolio">Portefølje</a>
          <a class="btn ghost" href="/blog">Blog</a>
          <a class="btn ghost" href="/contact">Kontakt</a>
        </div>

        <hr class="divider">

        <p class="muted" style="margin:0;">
          Åben for samarbejde og faglige samtaler – især om centralbanker, data og modeller.
        </p>
      </Card>
    </section>

    <!-- MINI ABOUT -->
    <Section class="card">
      <h2 class="section-title">Kort om mig</h2>
      <p class="muted">
        Jeg interesserer min særligt for, hvordan pengepolitik og økonomiske chok påvirker økonomien på tværs af lande,
        og hvordan modeller og data kan bruges til at gøre analysen mere præcis. På siden kan du finde mit CV,
        projekter og materiale.
      </p>
    </Section>
  </main>
</Layout>

<style>
.card {
  background: var(--glass);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-radius: var(--radius);
  padding: 24px;
  border: 1px solid var(--glass-border);
  box-shadow: var(--shadow);
  transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), border-color 0.3s ease;
}
.card:hover {
  transform: translateY(-4px) scale(1.01);
  border-color: var(--accent-soft);
}

.section-title {
  margin: 0 0 24px;
  font-size: 13px;
  text-transform: uppercase;
  letter-spacing: 0.2em;
  font-weight: 800;
  color: var(--accent);
}

.eyebrow {
  margin: 0 0 12px;
  color: var(--text-dim);
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  font-size: 12px;
}

.hero {
  display: grid;
  grid-template-columns: minmax(0, 1.6fr) minmax(0, 1fr);
  gap: 32px;
  align-items: center;
}

.hero h1 {
  margin: 0 0 20px;
  line-height: var(--lh-heading);
  letter-spacing: var(--ls-heading);
}

.lead { margin: 0 0 24px; color: var(--text-dim); font-size: 18px; line-height: 1.6; }

.hero-img {
  width: 100%;
  height: 380px;
  object-fit: cover;
  border-radius: 18px;
  display: block;
  margin-bottom: 16px;
  border: 1px solid var(--glass-border);
  box-shadow: var(--glow);
}

.hero-links a {
  display: inline-flex;
  gap: 10px;
  align-items: center;
  margin-right: 16px;
  text-decoration: none;
  font-weight: 500;
  color: var(--accent);
  transition: color 0.3s ease;
}

.hero-links a:hover {
  text-shadow: 0 0 10px rgba(96, 165, 250, 0.5);
  color: #fff;
}

.cta {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin: 10px 0 14px;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 12px 24px;
  border-radius: 999px;
  background: var(--accent);
  color: #050506;
  text-decoration: none;
  font-weight: 700;
  font-size: 14.5px;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
}
.btn:hover {
  transform: translateY(-2px);
  box-shadow: var(--glow);
}
.btn.ghost {
  background: transparent;
  border: 1.5px solid var(--accent);
  color: var(--accent);
}

.pill-wrap {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.grid-3 {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14px;
  margin-top: 14px;
  margin-bottom: 14px;
}

.clean-list {
  margin: 16px 0 0;
  padding-left: 0;
  list-style: none;
}

.clean-list li {
  margin-bottom: 12px;
  padding-left: 20px;
  position: relative;
  font-weight: 500;
  border-left: 1px solid var(--glass-border);
}

.clean-list li::before {
  content: "";
  position: absolute;
  left: -1px;
  top: 0;
  width: 1px;
  height: 100%;
  background: var(--accent);
  transform: scaleY(0);
  transition: transform 0.3s ease;
}

.clean-list li:hover::before {
  transform: scaleY(1);
}

.divider {
  border: 0;
  border-top: 1px solid var(--glass-border);
  margin: 20px 0;
}

.quick-links {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.home-wrap {
  /* No specific container width needed as it's handled by Section/Card usually,
     but we can add page-specific padding if needed */
}

/* Mobile */
@media (max-width: 980px) {
  .hero {
    grid-template-columns: 1fr;
  }

  .grid-3 {
    grid-template-columns: 1fr;
  }

  .home-wrap {
    padding-left: 14px;
    padding-right: 14px;
  }

  .hero-img {
    height: 240px;
  }
}

.home-wrap > :global(.card) {
  margin-top: 14px;
}

.home-wrap > :global(.card:first-child) {
  margin-top: 0;
}
</style>
