---
import DataMapPage from '@components/templates/DataMapPage.astro';
import { getEntry } from 'astro:content';

export const prerender = true;

const entry = await getEntry('pages', 'en/data-map');
if (!entry) throw new Error('Could not find content for en/data-map');
---

<DataMapPage lang="en" content={entry.data} />
