---
export const prerender = true;
import TrainingStatsPage from '@components/templates/TrainingStatsPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<TrainingStatsPage lang={lang} />
