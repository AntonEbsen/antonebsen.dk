---
import Layout from '../layouts/Layout.astro';
import Section from '../components/common/Section.astro';
import PodcastCard from '../components/resources/PodcastCard.astro';
import podcastsData from '../data/resources/podcasts.json';
import '../styles/podcasts.css';
---

<Layout 
  title="Anton Meier Ebsen Jørgensen – Favoritpodcasts" 
  description="En kurateret liste over mine favoritpodcasts – makro, data, træning, humor og refleksion."
  bodyClass="podcasts-page"
>

  <main class="podcasts-wrap">

    <!-- HERO -->
    <Section class="card hero">
      <div>
        <p class="eyebrow">Podcasts</p>
        <h1>Favoritter jeg faktisk hører igen</h1>
        <p class="lead">
          En kurateret liste opdelt efter tema. Jeg opdaterer løbende, når jeg finder noget der er værd at gemme.
        </p>

        <div class="pill-wrap" aria-label="Kategorier">
          <a class="pill-link" href="#cat-makro"><span class="pill">Makro</span></a>
          <a class="pill-link" href="#cat-data"><span class="pill">Data/Tech</span></a>
          <a class="pill-link" href="#cat-traening"><span class="pill">Træning</span></a>
          <a class="pill-link" href="#cat-humor"><span class="pill">Humor</span></a>
          <a class="pill-link" href="#cat-refleksion"><span class="pill">Refleksion</span></a>
        </div>

        <div class="cta">
          <a class="btn" href="#cat-makro"><i class="fa-solid fa-headphones"></i> Hop til listen</a>
          <a class="btn ghost" href="/contact"><i class="fa-solid fa-envelope"></i> Foreslå en podcast</a>
        </div>

        <p class="muted small" style="margin:0;">
          Note: Links kan være Spotify/Apple/YouTube. (Du kan vælge 1 platform pr. podcast for at holde det clean.)
        </p>
      </div>

      <div class="hero-side">
        <div class="mini-card">
          <h2 class="section-title">Hvordan jeg vælger</h2>
          <ul class="clean-list muted" style="margin:0;">
            <li>Høj signalværdi (ikke bare “snak”)</li>
            <li>Gode gæster / konkret viden</li>
            <li>Kan tåle genlyt</li>
            <li>Passer til pendling + gåture</li>
          </ul>
        </div>
      </div>
    </Section>

    <!-- KATEGORI: MAKRO -->
    <Section class="cat" id="cat-makro" title="Makro & pengepolitik">
      <p class="muted cat-subtitle" slot="before-content">Centralbanker, konjunkturer, policy og “hvad betyder det i praksis?”.</p>
      
      <div class="grid">
        {podcastsData.macro.map(pod => (
          <PodcastCard 
             title={pod.title}
             tag={pod.tag}
             reason={pod.reason}
             description={pod.description}
             links={pod.links}
          />
        ))}
      </div>
    </Section>

    <!-- KATEGORI: DATA -->
    <Section class="cat" id="cat-data" title="Data & tech">
      <p class="muted cat-subtitle" slot="before-content">Praktiske workflows, ML, programmering og værktøjer.</p>

      <div class="grid">
        {podcastsData.data.map(pod => (
          <PodcastCard 
             title={pod.title}
             tag={pod.tag}
             reason={pod.reason}
             description={pod.description}
             links={pod.links}
          />
        ))}
      </div>
    </Section>

    <!-- KATEGORI: TRÆNING -->
    <Section class="cat" id="cat-traening" title="Træning">
      <p class="muted cat-subtitle" slot="before-content">Progression, hypertrofi, vaner og sund fornuft.</p>

      <div class="grid">
        {podcastsData.training.map(pod => (
          <PodcastCard 
             title={pod.title}
             tag={pod.tag}
             reason={pod.reason}
             description={pod.description}
             links={pod.links}
          />
        ))}
      </div>
    </Section>

    <!-- KATEGORI: HUMOR -->
    <Section class="cat" id="cat-humor" title="Humor">
      <p class="muted cat-subtitle" slot="before-content">Hvis hjernen skal have pause (men stadig have kvalitet).</p>

      <div class="grid">
        {podcastsData.humor.map(pod => (
          <PodcastCard 
             title={pod.title}
             tag={pod.tag}
             reason={pod.reason}
             description={pod.description}
             links={pod.links}
          />
        ))}
      </div>
    </Section>

    <!-- KATEGORI: REFLEKSION -->
    <Section class="cat" id="cat-refleksion" title="Refleksion">
      <p class="muted cat-subtitle" slot="before-content">Samtaler, etik, tro, mening og perspektiv.</p>

      <div class="grid">
        {podcastsData.reflection.map(pod => (
          <PodcastCard 
             title={pod.title}
             tag={pod.tag}
             reason={pod.reason}
             description={pod.description}
             links={pod.links}
          />
        ))}
      </div>
    </Section>

    <!-- CTA -->
    <Section class="card" title="Har du en anbefaling?">
      <p class="muted">
        Hvis du har en podcast du mener passer til mine temaer, så send den gerne. Bonuspoint hvis du også sender 1 episode,
        der “sælger” den.
      </p>
      <div class="cta">
        <a class="btn" href="mailto:anton.ebsen@gmail.com?subject=Podcast%20anbefaling"><i class="fa-solid fa-paper-plane"></i> Send anbefaling</a>
        <a class="btn ghost" href="/blog"><i class="fa-solid fa-arrow-left"></i> Tilbage til blog</a>
      </div>
    </Section>

  </main>
</Layout>
