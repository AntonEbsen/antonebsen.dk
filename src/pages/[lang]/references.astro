---
export const prerender = true;
import ReferencesPage from '@components/templates/ReferencesPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<ReferencesPage lang={lang} />
