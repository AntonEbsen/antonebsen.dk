---
export const prerender = true;
import AiProjectPage from '@components/templates/AiProjectPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<AiProjectPage lang={lang} />
