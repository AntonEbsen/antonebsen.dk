---
// /ai was a second, worse chat page. It called `gemini-pro` — a retired model id, so
// every request fell straight into its catch and rendered "Neural Link Unstable" — it
// interpolated the visitor's query into the prompt with no rate limit in front of it,
// and its persona described Anton as a "Software Engineer and Hybrid Athlete", which
// contradicts every other prompt on the site.
//
// /ai-chat is the maintained full-page assistant. Nothing internal linked here, so this
// redirect exists only for bookmarks and external links.
export const prerender = false;

return Astro.redirect('/ai-chat', 301);
---
