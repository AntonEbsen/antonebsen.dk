---
export const prerender = true;
import NotesPage from '@components/templates/NotesPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<NotesPage lang={lang} />
