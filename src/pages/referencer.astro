---
import Layout from '../layouts/Layout.astro';
import Section from '@components/ui/Section.astro';
import Card from '@components/ui/Card.astro';
import Button from '@components/ui/Button.astro';
import SectionHeading from '@components/ui/SectionHeading.astro';
import ContactCard from '@components/common/ContactCard.astro';
---

<Layout 
  title="Anton Meier Ebsen Jørgensen – Anbefalinger" 
  description="Anbefalinger og udtalelser om mit arbejde inden for økonomi, data og undervisning."
  bodyClass="testimonials-page"
>
  <style>
    .testimonials-wrap {
      max-width: 1200px;
      margin: 24px auto;
      padding: 0 16px 32px;
    }

    .hero-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.6fr) minmax(0, 1fr);
      gap: 22px;
      align-items: start;
    }

    .quotes-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 14px;
      margin-bottom: 14px;
    }

    @media (max-width: 980px) {
      .hero-grid, .quotes-grid { 
        grid-template-columns: 1fr; 
      }
    }
  </style>

  <main class="testimonials-wrap">

    <!-- HERO -->
    <Section>
      <div class="hero-grid">
        <Card variant="glass" class="p-8">
          <p class="text-xs font-bold uppercase tracking-widest text-dim mb-3">Anbefaliner</p>
          <h1 class="text-4xl font-bold leading-tight mb-4">Udtalelser om mit arbejde</h1>
          <p class="text-lg text-dim mb-6">
            Her kan du læse korte udtalelser fra samarbejdspartnere og personer, jeg har arbejdet sammen med.
            (Hvis du vil tilføje flere, kan vi også lave en “kort version” + link til LinkedIn-anbefalinger.)
          </p>
          <div class="flex flex-wrap gap-3">
            <Button href="/contact">Kontakt</Button>
            <Button href="/cv" variant="ghost">Se CV</Button>
            <Button href="https://www.linkedin.com/in/antonebsen/" target="_blank" rel="noopener" variant="ghost">LinkedIn</Button>
          </div>
        </Card>

        <div class="hidden md:block">
          <Card variant="glass" class="p-6">
            <SectionHeading>Fokus</SectionHeading>
            <ul class="list-none p-0 m-0 space-y-2 text-sm text-dim opacity-90">
              <li><strong>Analytisk styrke & struktur</strong></li>
              <li><strong>Høj kvalitet og deadline-disciplin</strong></li>
              <li><strong>Formidling og samarbejde</strong></li>
            </ul>
          </Card>
        </div>
      </div>
    </Section>

    <!-- TESTIMONIAL GRID -->
    <Section class="quotes-grid" aria-label="Anbefalinger">

      <Card variant="glass" class="p-6 relative">
        <div class="absolute top-4 left-6 text-4xl text-accent opacity-10 select-none font-serif">“</div>
        <p class="text-base text-text italic mt-4 mb-6 relative z-10 leading-relaxed">
          Anton er en exceptionel professionel, der konsekvent leverer resultater af høj kvalitet.
          Hans analytiske evner og sans for detaljer er uovertrufne.
        </p>
        <div class="border-t border-glass-border pt-4">
          <div class="font-bold">John Doe</div>
          <div class="text-xs text-dim">CEO, ABC Corp</div>
        </div>
      </Card>

      <Card variant="glass" class="p-6 relative">
        <div class="absolute top-4 left-6 text-4xl text-accent opacity-10 select-none font-serif">“</div>
        <p class="text-base text-text italic mt-4 mb-6 relative z-10 leading-relaxed">
          At arbejde med Anton har været en fantastisk oplevelse. Han bringer kreativitet og dedikation til hvert projekt,
          hvilket sikrer succes hver gang.
        </p>
        <div class="border-t border-glass-border pt-4">
          <div class="font-bold">Jane Smith</div>
          <div class="text-xs text-dim">Marketing Manager, XYZ Ltd.</div>
        </div>
      </Card>

      <Card variant="glass" class="p-6 relative">
        <div class="absolute top-4 left-6 text-4xl text-accent opacity-10 select-none font-serif">“</div>
        <p class="text-base text-text italic mt-4 mb-6 relative z-10 leading-relaxed">
          Anton har en utrolig evne til at nedbryde komplekse problemer og finde innovative løsninger.
          Jeg kan varmt anbefale ham til ethvert projekt.
        </p>
        <div class="border-t border-glass-border pt-4">
          <div class="font-bold">Emily Brown</div>
          <div class="text-xs text-dim">Product Manager, DEF Inc.</div>
        </div>
      </Card>

    </Section>

    <!-- OPTIONAL: CTA CARD -->
    <Section>
        <ContactCard 
            title="Vil du give en anbefaling?"
            text="Hvis vi har arbejdet sammen, er du meget velkommen til at sende en kort udtalelse (2–4 linjer), som jeg kan tilføje her – eller skrive en anbefaling på LinkedIn."
            action="linkedin"
        />
    </Section>

  </main>
</Layout>
