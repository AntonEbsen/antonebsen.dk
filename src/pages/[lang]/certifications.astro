---
import CertificationsPage from '@components/templates/CertificationsPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<CertificationsPage lang={lang} />
