---
import QuotesPage from '@components/templates/QuotesPage.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params;
---
<QuotesPage lang={lang} />
